node_modules/
.wrangler/
package-lock.json
# پوشه‌های موقتِ تست‌ها (worker-e2e و ip-limit هنگام اجرا می‌سازند و پاک می‌کنند؛
# اگر اجرای تست نیمه‌کاره بماند، این‌ها باقی می‌مانند و نباید کامیت شوند)
.e2e-tmp/
.iplimit-tmp/
.tunnellimit-tmp/
.updcheck-tmp/
# باینریِ Xray برای تستِ زندهٔ واقعیت (reality-live-e2e) — چند ده مگابایت است
# و هر کس خودش دانلود می‌کند؛ هرگز کامیت نشود
tests-manual/.e2e/
# پیامِ کامیتِ موقت (فقط برای اسکریپت‌ها)
.commitmsg.txt
