{
  "name": "sub-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Build pipeline: obfuscate worker.js before deploying to Cloudflare (anti-1101 scanner hardening, like byJoey/cfnew)",
  "scripts": {
    "build": "node scripts/build-obfuscated.mjs",
    "deploy": "npm run build && npx wrangler deploy",
    "deploy:plain": "npx wrangler deploy"
  },
  "devDependencies": {
    "javascript-obfuscator": "^4.1.1"
  }
}
