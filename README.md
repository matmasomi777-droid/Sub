# پنل مدیریت کانفیگ روی Cloudflare Workers

<div dir="rtl">

پنلی تک‌فایل برای ساخت، مدیریت و توزیع کانفیگ‌های **VLESS / Trojan / Shadowsocks / VMess** روی لبه‌ی شبکه‌ی کلاودفلر — بدون سرور، بدون npm، بدون هزینه.

> **نام پروژه:** Panel Core • **نسخه:** 2.0.0 • **زبان:** JavaScript (Workers) • **رابط:** فارسی/RTL

---

## ✨ معرفی

این پروژه یک ورکر Cloudflare است که سه کار انجام می‌دهد:

1. **هسته‌ی تونل** — اتصال VLESS و Trojan روی WebSocket را واقعاً می‌پذیرد و ترافیک را با `cloudflare:sockets` به مقصد می‌رساند.
2. **پنل مدیریت** — رابط کاربری کامل (تک‌صفحه‌ای، فارسی، تم تاریک/روشن) که **از گیت‌هاب خوانده می‌شود** (مثل نهان): فایل‌های `ui/index.html`، `ui/style.css`، `ui/app.js`.
3. **سرویس اشتراک** — لینک ساب‌اسکریپشن با ۶ فرمت خروجی و تشخیص خودکار کلاینت.

### چرا این معماری؟
فایل‌های UI جدا از موتور هستند؛ یعنی می‌توانید ظاهر پنل را عوض کنید **بدون دست زدن به ورکر** — کافی است فایل‌های css/html را در مخزن گیت‌هاب تغییر دهید (قابلیت **FR** در تنظیمات).

---

## 🚀 نصب

### روش ۱ — داشبورد کلاودفلر با نسخهٔ ضد-1101 (توصیه‌شده)

> **چرا این روش؟** فایل `worker.js` خام، رشته‌های پروتکل (`vless`، `trojan` و…) را به‌صورت متن ساده دارد و می‌تواند اسکنر استاتیک کلاودفلر را فعال کند (نتیجه: خطای ۱۱۰۱ یا محدودسازی حساب). نسخهٔ obfuscate شده هیچ رشتهٔ پروتکلی ندارد — همان روشی که پروژه‌های مثل byJoey/cfnew استفاده می‌کنند.

**گام ۱ — ساخت نسخهٔ obfuscate شده:**

```bash
npm install
npm run build
```

خروجی: فایل **`_worker.obf.js`** (حدود ۵ مگابایت). این فایل نسخهٔ امن و قابل‌دیپلوی است — و **از قبل ساخته‌شده در همین مخزن** هم موجود است؛ اگر نمی‌خواهید Node اجرا کنید، همان فایلِ موجود در ریپو را بردارید.

**گام ۲ — دیپلوی در داشبورد:**

1. وارد <https://dash.cloudflare.com> شوید
2. **Workers & Pages → Create application → Create Worker**
3. نام را `panel` بگذارید → **Deploy**
4. روی ورکر کلیک کنید → **Edit code**
5. کل محتوای ادیتور را پاک کنید و **تمام فایل `_worker.obf.js`** را جایگذاری کنید (`Ctrl+A` → `Ctrl+V`)
6. **Deploy** را بزنید و آدرس `https://panel.<account>.workers.dev` را باز کنید

> رمز پیش‌فرض: **`simorgh`**

> ⚠️ **نکتهٔ obfuscation:** چون کد obfuscate شده، اشکال‌زدایی (debug) سخت است. برای عیب‌یابی، موقتاً فایل `worker.js` خام را پیست کنید، مشکل را پیدا و رفع کنید، بعد دوباره `npm run build` بزنید.

### روش ۲ — داشبورد کلاودفلر با نسخهٔ خام (فقط برای عیب‌یابی)

همان مراحل روش ۱، ولی فایل [`worker.js`](./worker.js) را جایگذاری کنید. **توصیه نمی‌شود** — رشته‌های پروتکلِ خام ممکن است پرچم بخورد.

### مرحله‌ی مهم: بارگذاری UI از گیت‌هاب

رابط کاربری از گیت‌هاب خوانده می‌شود:

1. یک مخزن بسازید و چهار فایل پروژه را در آن بگذارید:
   ```
   ui/index.html
   ui/style.css
   ui/app.js
   ```
2. در پنل وارد شوید → **پروتکل و کانفیگ → Fragment و FR**
3. مقدار `fr.repo` را به `username/repo` تغییر دهید و ذخیره کنید
4. صفحه را رفرش کنید (دکمه‌ی «بازخوانی» در **تنظیمات و پشتیبان → فایل‌های UI**)

> اگر گیت‌هاب در دسترس نباشد، ورکر پیام «بارگذاری رابط کاربری ناموفق بود» نشان می‌دهد. برای تست محلی می‌توانید فایل‌ها را در مخزن خودتان میزبانی کنید.

### 🚨 مرحله‌ی حیاتی: بایندکردنِ مرجعِ محدودیت (وگرنه سقفِ آی‌پی کار نمی‌کند)

**این مهم‌ترین مرحله‌ی استقرار است.** محدودیتِ آی‌پی (سقفِ نفراتِ هر کانفیگ)
فقط وقتی کار می‌کند که یک **مرجعِ مشترک بین isolateها** بایند شده باشد. اگر
هیچ‌کدام بایند نباشد، هر isolate حافظه‌ی خودش را می‌شمارد، اتصالِ سوم به یک
isolate تازه می‌افتد، از صفر شمرده می‌شود و **اجازه می‌گیرد** — یعنی سقف
عملاً بی‌اثر است، **بی‌صدا و بدون هیچ خطایی**.

> ⚠️ **تاریخچه‌ی این باگ (تا دوباره تکرار نشود):** تا قبل از کامیتِ
> `360e05c`، فایل `wrangler.toml` این بایندینگ‌ها را تعریف می‌کرد و محدودیت
> دقیق کار می‌کرد. آن کامیت `wrangler.toml` را حذف کرد تا استقرار از طریق
> «پیست در داشبورد» ممکن شود — ولی بایندینگ‌ها هم با آن ناپدید شدند. از آن
> لحظه محدودیت بدون هیچ پیامِ خطایی از کار افتاد. حالا `wrangler.toml` برگشته
> و پنل هم اگر مرجع بایند نباشد یک **بنرِ قرمز** نشان می‌دهد.

**یکی از این‌ها را انتخاب کنید:**

**راهِ الف — D1 (توصیه‌شده، همه‌چیز در داشبورد):**

1. **Workers & Pages → D1 → Create database** → نامی مثل `panel-db` بدهید
2. برگردید به **ورکر → Settings → Bindings → Add → D1 database**
3. **Variable name** را دقیقاً `DB` بگذارید و همان پایگاه را انتخاب کنید
4. **Deploy** را بزنید — جدولِ `conns` خودکار ساخته می‌شود

**راهِ ب — Durable Object (دقیق‌ترین، از طریق wrangler):**

```bash
npx wrangler login
npx wrangler secret put MASTER_KEY
npx wrangler deploy        # بایندینگِ LIMITER از wrangler.toml خوانده می‌شود
```

**راهِ ج — KV (ساده‌ترین، اما تقریبی):**

**Worker → Settings → Bindings → Add → KV Namespace → Variable name: `KV`**

KV بین isolateها مشترک است ولی eventually-consistent؛ زیرِ بارِ سنگین ممکن است
سقف با تأخیر اعمال شود. برای دقتِ کامل از D1 استفاده کنید.

| مرجع | سراسری؟ | دقت | چطور ببندیم |
|---|---|---|---|
`LIMITER` (Durable Object) | ✓ | **دقیق** | `wrangler.toml` + `npx wrangler deploy` |
`DB` (D1) | ✓ | **دقیق** | داشبورد → Bindings → D1 database |
`KV` | ✓ | تقریبی | داشبورد → Bindings → KV Namespace |
هیچ‌کدام | ✗ | **بی‌اثر** | ❌ سقفِ آی‌پی اعمال نمی‌شود |

**بررسیِ اینکه درست بایند شده:** پنل → **سلامت ورکر → مرجعِ شمارشِ محدودیت**
(باید «Durable Object» یا «D1» با تیکِ سبز باشد). بدون ورود هم:

```
https://panel.<account>.workers.dev/health        (یا /api/health — هر دو یکی‌اند)
```

فیلد `limiter` را ببینید: `do` یا `d1` = سالم • `kv` = تقریبی • `mem` = **سقف اعمال نمی‌شود**.
فیلد `limitEnforced` هم مستقیم جواب می‌دهد: `true` = اعمال می‌شود، `false` = **نمی‌شود**.

### مرحله‌ی مهم: ذخیره‌سازی پایدار (KV)

بدون KV یا D1، کاربران و تنظیمات با سرد شدن ورکر **ریست می‌شوند**:

از داشبورد: **Worker → Settings → Bindings → Add → KV Namespace → Variable name: `KV`**

> اگر مرجعِ محدودیت را D1 گذاشتید (راهِ الف)، همان `DB` هم ماندگاری را
> تأمین می‌کند و KV اختیاری می‌شود.

متغیرهای اختیاری:

| متغیر | توضیح |
|---|---|
`MASTER_KEY` | رمز ورود پنل (بر ترتیب اولویت از تنظیمات داخلی) |

---

## 🧭 استفاده

### ۱) ساخت کاربر
**کاربران → کاربر جدید** → نام، سهمیه و انقضا را بدهید. UUID و رمز خودکار ساخته می‌شوند.

هر کاربر می‌تواند تنظیمات اختصاصی داشته باشد: حالت (Alpha/Beta/Both)، پورت‌ها، IPهای پاک، Proxy IP، Node، NAT64، Panel URL، محدودیت اتصال/IP/کانفیگ، سرعت و بلاک محتوا.

### ۲) گرفتن لینک ساب
**اشتراک** → کاربر را انتخاب کنید → کپی لینک یا QR.

```
https://panel.<account>.workers.dev/sub/<uuid>
```

| کلاینت | فرمت خودکار |
|---|---|
v2rayNG / v2rayN / Shadowrocket | Base64
Hiddify / Karing / Happ / Sing-box | Sing-box JSON
Clash Meta / Mihomo / FlClash | Clash Meta JSON
Clash | YAML
V2RayN (JSON) | V2Ray JSON |

تحمیل صریح فرمت: `?format=base64|raw|clash|meta|singbox|v2ray`

### ۳) افزودن کاربر در کلاینت
- **v2rayNG:** آیکون + → «کپی از کلیپ‌بورد» (لینک ساب را کپی کنید) → آیکون ⋮ → «به‌روزرسانی اشتراک»
- **Hiddify:** افزودن پروفایل → «لینک اشتراک» را بچسبانید
- **Clash Meta:** Profiles → لینک را وارد کنید

### ۴) صفحه‌ی وضعیت کاربر
```
https://panel.<account>.workers.dev/status/<username>
```
مصرف، باقیمانده و تاریخ انقضا را بدون ورود نشان می‌دهد.

---

## 🛰 اسکنر آی‌پی تمیز (رادار)

در پایین صفحه‌ی کاربر یک کارتِ **رادار آی‌پی تمیز** هست که کاملاً در مرورگر اجرا می‌شود: رنج‌های رسمیِ کلودفلر را می‌گردد، آی‌پی‌های زنده را پیدا می‌کند و بر اساس پینگ مرتب می‌کند، بعد آن‌ها را در پنل ذخیره و روی کانفیگ‌های همان کاربر اعمال می‌کند.

**تنظیمات:** پنل → **پیکربندی → اسکنر آی‌پی**

| تنظیم | پیش‌فرض | توضیح |
|---|---|---|
`enabled` | روشن | نمایش/پنهان‌کردن کارت اسکنر در صفحه‌ی کاربر |
`ipCount` | **۲۰۴۸** | تعداد آی‌پی‌های هر اسکن |
`concurrency` | **۱۶** | تعداد پروب‌های موازی (بالاتر = سریع‌تر، ولی فشارِ بیشتر روی شبکه) |
`timeout` | **۲۰۰۰ ms** | تایم‌اوت هر پروب — زیرِ ۱۵۰۰ms روی شبکه‌ی موبایل خیلی از لبه‌های سالم «مرده» حساب می‌شوند |
`probes` | **۳** | تعداد پروب برای تأییدِ هر آی‌پی (میانگین + لرزش + افت) |
`autoFloor` | **روشن** | کفِ خودکارِ تأخیر — تأخیرِ پایه تا دامنه‌ی ورکر اندازه‌گیری می‌شود و پاسخ‌های بی‌درنگ (RSTِ فیلترشکن/میان‌راه) رد می‌شوند. **همین تنظیم است که «آی‌پی پیدا می‌شود ولی در کانفیگ کار نمی‌کند» را رفع می‌کند** |
`minRtt` | ۰ | کفِ دستیِ تأخیر — ۰ = فقط کفِ خودکار. اگر در نتیجه پاسخ‌های بسیار سریع (زیر ۲۰ms) می‌بینید، اینجا مقدار بگذارید |
`maxRtt` | ۰ | حداکثر تأخیرِ قابل‌قبول — ۰ = بدون سقف |
`keep` | ۰ | تعداد آی‌پیِ ذخیره‌شده — ۰ = سقفِ کانفیگِ کاربر |
`mode` | `smart` | `smart` = سهمِ متناسب با اندازه‌ی رنج + کفِ تضمینی برای هر رنج • `even` = سهمِ برابر برای هر رنج • `random` = تصادفیِ وزنی |
`ports` | خالی | پورت‌های اسکن — خالی = پورت‌های TLS خودِ کانفیگ‌های کاربر (و اگر پیدا نشد، ۴۴۳) |
`ranges` | خالی | رنج‌های CIDR دلخواه — **به** فهرست رسمی اضافه می‌شوند |

### رنج‌های اسکن‌شده
همیشه **تمامِ** رنج‌های رسمیِ IPv4 کلودفلر (مستقیماً از <https://www.cloudflare.com/ips-v4>) اسکن می‌شوند — نه زیرمجموعه و نه بازه‌ی دستی:

```
173.245.48.0/20    103.21.244.0/22   103.22.200.0/22   103.31.4.0/22
141.101.64.0/18    108.162.192.0/18  190.93.240.0/20   188.114.96.0/20
197.234.240.0/22   198.41.128.0/17   162.158.0.0/15    104.16.0.0/13
104.24.0.0/14      172.64.0.0/13     131.0.72.0/22
```

### اجرای اسکن از خودِ پنل
لازم نیست صفحه‌ی کاربر را باز کنید: در **پیکربندی → اسکنر آی‌پی** کارتِ **اجرای اسکن از پنل** هست.

- **شروع اسکن / توقف** — با نوارِ پیشرفت و شمارنده‌ی زنده‌ی آی‌پی‌های سالم.
- **جدولِ نتیجه:** آی‌پی:پورت • تأخیر • لرزش • افت — مرتب‌شده بر اساس بهترین امتیاز.
- **اعمال و ذخیره در «IPهای پاک»** — آی‌پی‌های برتر اولِ فهرستِ `cleanIPs` می‌نشینند (بدون تکرار، سقف ۱۰۰) و همان لحظه ذخیره می‌شوند.
- مقادیرِ **همین صفحه** استفاده می‌شوند (تعداد، هم‌روندی، تایم‌اوت، پورت‌ها، رنج‌ها) — لازم نیست اول ذخیره کنید تا یک اسکنِ آزمایشی بگیرید.
- پروب از مرورگرِ خودتان انجام می‌شود، پس نتیجه همان چیزی است که کاربرانتان می‌بینند.

### چطور کار می‌کند
- **پروبِ دوکاناله (`Image` + `fetch`) —** روی `https://<ip>:<port>/cdn-cgi/trace`. گواهیِ آی‌پیِ خام هیچ‌وقت معتبر نیست، پس مرورگر بعد از TLS اتصال را رد می‌کند و «سرعتِ رسیدنِ خطا» خودش سیگنالِ زنده‌بودنِ لبه است:
  - خطا **پیش از** تایم‌اوت = لبه پاسخ داد = **سالم**
  - **تایم‌اوت** = هیچ پاسخی نیامد = **مرده**

  هر دو کانال **موازی** فرستاده می‌شوند و اولین سیگنال برنده است. چرا هر دو: تجربه‌ی عملی نشان داده هیچ‌کدام به‌تنهایی قابل‌اعتماد نیست —
  - نسخه‌ی اصلیِ همین پنل اول `Image` داشت و بعد به `fetch` مهاجرت کرد، چون «RST آنیِ فیلترشکن/مسیرِ مسدود هم زنده حساب می‌شد».
  - پنل [نوا](https://github.com/norayanmostafa/nova) برعکس، `fetch` را رها کرد و به `Image` رفت.
  - اسکنرِ [IRCF](https://ircfspace.github.io/tester/) با `fetch` و اسکنرِ [SenPai](https://github.com/MatinSenPai/SenPaiScanner) با Go کار می‌کنند.

  اگر یکی از دو کانال در مرورگر/شبکه‌ی کاربر از کار بیفتد، دیگری اسکن را نجات می‌دهد و نتیجه بی‌صدا خالی نمی‌ماند.
- **کفِ تأخیر — مهم‌ترین فیلترِ کیفیت:** پیش از هر اسکن، یک درخواستِ کامل به **دامنه‌ی خودِ ورکر** زده می‌شود. این دامنه روی کلادفلر است، پس تأخیرِ اندازه‌گیری‌شده یک «لبه‌ی واقعی» است. سپس هر پاسخی که **بسیار سریع‌تر** از آن باشد رد می‌شود (`کف = max(۲۰ms, ۴۰٪ تأخیرِ پایه)` با سقفِ ۲۰۰ms).
  چرا حیاتی است: `RSTِ` آنیِ فیلترشکن/میان‌راه در حدودِ ۱ms می‌رسد و بدونِ این کف، رتبه‌بندیِ «سریع‌ترین اول» باعث می‌شود **آی‌پی‌های خراب جای آی‌پی‌های سالم را در کانفیگ بگیرند** — یعنی «آی‌پی پیدا می‌شود ولی در کانفیگ کار نمی‌کند». تعدادِ رد‌شده‌ها در خطِ وضعیت (`مردودِ سریع`) و در پنل دیده می‌شود.
- **دروازه‌ی ثبات:** آی‌پی‌ای که از `probes` پروب فقط یک‌بار پاسخ بدهد پذیرفته نمی‌شود (حداقل ۲ پاسخ لازم است). لبه‌ی واقعی هر بار پاسخ می‌دهد؛ مسیرِ نیمه‌باز (که DPI بعد از اولین درخواست می‌بندد) نه.
- **پورت‌ها موازی** آزموده می‌شوند تا یک آی‌پیِ مرده فقط **یک** تایم‌اوت هزینه بدهد؛ پروب‌های تکمیلی فقط برای پورتِ برنده اجرا می‌شوند (میانگین، لرزش، افت، امتیاز = `avg + jitter×0.5 + loss×20`). فقط پورت‌های TLS (۴۴۳ • ۲۰۵۳ • ۲۰۸۳ • ۲۰۸۷ • ۲۰۹۶ • ۸۴۴۳) معنی دارند — پورتِ غیر-TLS با خطای SSL بی‌درنگ «پاسخ» می‌دهد و همه‌چیز را زنده نشان می‌دهد.
- **فاصله‌ی تصادفیِ ۱۰ تا ۶۰ میلی‌ثانیه** بین پروب‌های یک آی‌پی — الگوی درخواست‌ها کمتر شبیه اسکنر می‌شود و DPI کمتر اتصال را قطع می‌کند (روشِ [SenPai Scanner](https://github.com/MatinSenPai/SenPaiScanner)). همین چند پروب هم افتِ بعد از اولین درخواست (که روی ISPهای ایران رایج است) را نشان می‌دهد.
- **خودآزماییِ پروب:** پیش از هر اسکن، پروب روی سه آی‌پیِ آزمایشیِ RFC 5737 (`192.0.2.1` • `198.51.100.1` • `203.0.113.1`) اجرا می‌شود. این بازه‌ها هرگز مسیریابی نمی‌شوند، پس اگر پروب به آن‌ها «پاسخ» بدهد یعنی هر خطای سریعِ محلی دارد «زنده» تفسیر می‌شود و نتیجه بی‌اعتبار است — در آن حالت هشدار داده می‌شود. (کفِ خودکارِ تأخیر معمولاً همین مشکل را خودکار خنثی می‌کند؛ `minRtt` فقط برای سخت‌گیریِ دستیِ بیشتر است.)
- **شمارنده‌ی پاسخ:** وضعیتِ اسکن تعدادِ «پاسخ‌های خام» را جدا از تعدادِ آی‌پی‌های پذیرفته‌شده نشان می‌دهد. اگر اسکن با صفر نتیجه تمام شود، پیام تفکیک می‌کند: «هیچ آی‌پی پاسخ نداد» (شبکه اتصالِ مستقیم TLS به آی‌پیِ خام را می‌بندد) در مقابل «پاسخ آمد ولی فیلتر شد» (`minRtt`/`maxRtt`).
- **تخصیصِ بودجه (`smart`):** سهمِ هر رنج به‌تناسبِ اندازه‌اش است، با یک کفِ تضمینی تا رنج‌های کوچک هم اسکن شوند. دلیل: در فهرست‌های عمومیِ آی‌پیِ تمیز، حدود **۹۶٪** آی‌پی‌های سالم در ۳ رنجِ بزرگ (`104.16.0.0/13` • `172.64.0.0/13` • `104.24.0.0/14`) هستند و **۹ رنجِ دیگر تقریباً هیچ آی‌پیِ سالمی ندارند**. حالتِ `even` حدود ۸۰٪ بودجه را خرجِ همان ۹ رنج می‌کرد؛ `smart` در ۲۰۴۸ نمونه ~۸۳٪ بودجه را به ۳ رنجِ پربازده می‌دهد (در مقابل ~۲۰٪ برای `even`) و هیچ رنجی را صفر نمی‌گذارد.
- **ذخیره:** آی‌پی‌های برتر با `POST /<sub-path>/<user-id>/radar-ips` ذخیره می‌شوند — روی کانفیگ‌های همان کاربر و در «شبکه ← IPهای پاک» پنل.
- **لاگ:** هر اسکن (موفق یا بی‌نتیجه) در **لاگ → رادار** ثبت می‌شود.

### محدودیتِ ذاتیِ اسکنِ مرورگری
مرورگر نمی‌تواند SNI را برای یک آی‌پیِ خام تعیین کند و گواهیِ آی‌پی هم معتبر نیست، پس **نمی‌توان بدنه‌ی `/cdn-cgi/trace` را خواند یا `CF-Ray`/`colo` را دید**. یعنی اسکنِ مرورگری فقط می‌تواند بگوید «روی این آی‌پی در یک رنجِ رسمیِ کلودفلر، سرویسِ TLS پاسخ داد» — نه اینکه «این دقیقاً یک لبه‌ی کلودفلر است».

> **مهم — چرا روی بعضی شبکه‌ها نتیجه صفر است:** پروبِ مرورگری TLS را **بدون SNI** می‌فرستد (آی‌پیِ خام). روی شبکه‌های ایران، DPI اغلب دقیقاً همین الگو را می‌بندد، در حالی که اتصالِ واقعیِ پروکسی (با SNI درست) سالم کار می‌کند. نتیجه: آی‌پی‌ها واقعاً کار می‌کنند ولی اسکنرِ همان دستگاه پیدایشان نمی‌کند. **راه‌حل: اسکن را از یک VPS/شبکه‌ی تمیز انجام دهید** — یا از کارتِ «اجرای اسکن از پنل» استفاده کنید. اگر `timeout` را بالا ببرید و همچنان شمارنده‌ی «پاسخ» صفر بماند، همین محدودیت است، نه خرابیِ اسکنر.

این برای هدفِ ما کافی است (رنج‌ها از پیش فقط کلودفلر هستند)، ولی اگر اعتبارسنجیِ قطعی می‌خواهید، [SenPai Scanner](https://github.com/MatinSenPai/SenPaiScanner) همین کار را بومی (با Xray و خواندن `colo`) انجام می‌دهد.

> تست بدون مرورگر: `node tests-manual/scanner-engine.cjs` (موتورِ صفحه‌ی کاربر) و `node tests-manual/panel-scanner.cjs` (اسکنرِ داخلِ پنل) و `node tests-manual/worker-e2e.cjs` (e2e ورکر + فالبک‌ها)

---

## ⚙️ قابلیت‌ها

| بخش | امکانات |
|---|---|
**پروتکل** | VLESS (Alpha) • Trojan با SHA-224 (Beta) • Both • Shadowsocks • VMess • تقسیم مساوی چندپروتکله
**ترنسپورت** | WebSocket • gRPC • XHTTP • TCP Fast Open • Mux
**امنیت لایه‌ی انتقال** | TLS • uTLS fingerprint (randomized و…) • ECH • ALPN
**ضد فیلتر** | Fragment بسته (Shadowrocket/Happ/Custom) • Noise • جانک تصادفی مسیر • NAT64
**شبکه** | Clean IP با نام‌گذاری `ip#name` • Proxy IP با failover • Backup/Custom Relay • استخر per-ISP • GeoIP • race dial • DoH اختصاصی + پروکسی `/dns-query`
**اسکنر** | رادار آی‌پی تمیز سمت مرورگر • ۲۰۴۸ آی‌پی در هر اسکن • تمامِ رنج‌های رسمی کلودفلر با تخصیصِ هوشمندِ بودجه • پروبِ دوکاناله (`Image` + `fetch`) • خودآزماییِ پروب + شمارنده‌ی پاسخ • اجرای اسکن از خودِ پنل با جدولِ نتیجه • ذخیره‌ی خودکار روی کانفیگ کاربر • تنظیمات کامل از پنل
**کاربران** | سهمیه کل/روزانه • انقضا با غیرفعال‌سازی خودکار • محدودیت اتصال/IP/کانفیگ • speed limit • ریست مصرف
**اشتراک** | ۶ فرمت • تشخیص UA • کانفیگ فیک مصرف/انقضا • خط کانال تلگرام • converter API • قواعد روتینگ DOMAIN/IP-CIDR/GEOIP/GEOSITE • گروه‌بندی کشور
**ربات تلگرام** | ارسال واقعی اعلان (ورود، کاربر جدید، هشدارها) + ۲۰+ فرمان مدیریتی
**مانیتورینگ** | مصرف آپلود/دانلود هر کاربر • سری روزانه/ماهانه/سالانه • شمارش درخواست • uptime • نسخه/بیلد
**به‌روزرسانی** | بررسی واقعی از GitHub Releases • مقایسه نسخه • استقرار • rollback • انتشار به نودها
**چندپنلی** | Hub & Spoke • کلید per-node • همگام‌سازی • login signal
**امنیت پنل** | توکن JWT-مانند (HMAC-SHA256، ۲۴ ساعت) • 2FA واقعی (TOTP) • rate limit ۵ در ۱۰ دقیقه • چرخش مسیر ورود • Disguise با ۴ سایت پوششی • CSP/XFO/nosniff/CORS • Panic Mode • Kill Switch
**سیستمی** | کلیدهای API (تا ۱۰) • لاگ ۵۰ رویدادی • Backup/Restore • ریست کارخانه‌ای • health check دامنه

---

## 🔒 محدودیت IP همزمان (سقف نفراتِ هر کانفیگ)

سقف روی **تعداد IPهای همزمانِ هر کاربر** حساب می‌شود، نه تعداد کانکشن: با سقفِ
«۱»، اولین IP وصل می‌شود و اتصال‌های بعدیِ *همان* IP هم آزادند؛ فقط IP دوم رد
می‌شود. مقدار هر کاربر بر پیش‌فرضِ سراسری (`sec.ipConnLimit`) اولویت دارد.

### چرخه‌ی عمر یک «سهمیه»

| رویداد | نتیجه |
|---|---|
قطع شدنِ اتصال (close / خطا / لغو) | آزادسازی **آنی** — جای IP همان لحظه خالی می‌شود
اتصالِ باز ولی **بی‌ترافیک** (گوشی با صفحه‌ی خاموش، لپ‌تاپ در فاصله‌ی دو صفحه) | سهمیه **نگه داشته می‌شود** (ضربانِ دوره‌ای)
اتصالِ فعال | با هر بایت تمدید می‌شود
قطعیِ **بی‌خبر** (kill شدنِ isolate، قطعِ ناگهانیِ موبایل، بدون رویدادِ close) | ردیف بعد از `CONN_TTL` خودبه‌خود پاک می‌شود

> ⚠️ این تفکیک همان باگی است که قبلاً سقف را بی‌اثر می‌کرد: نسخه‌های قدیمی یک
> پنجره‌ی ۳ ثانیه‌ای داشتند که «بی‌ترافیک» را با «مرده» یکی می‌گرفت، پس ردیفِ
> تونلِ ساکت پاک می‌شد و جایش به IP تازه داده می‌شد — یعنی با سقفِ «۱»، عملاً
> بی‌شمار IP می‌توانستند پشتِ سرِ هم وصل شوند.

### تنظیمِ دقیق (بالای `worker.js`)

| ثابت | پیش‌فرض | کار |
|---|---|---|
`CONN_TTL` | ۹۰ ثانیه | مهلتِ بقای ردیفِ اتصال (شبکه‌ی ایمنی برای قطعیِ بی‌خبر)
`CONN_KEEPALIVE_MS` | ۴۵ ثانیه | ضربانِ اتصال‌های بازِ بی‌ترافیک (نصفِ مهلت → تحملِ یک تیکِ ازدست‌رفته)
`CONN_ACTIVITY_MS` | ۳۰ ثانیه | سقفِ نرخِ تمدیدِ مبتنی بر ترافیک (کنترلِ هزینه‌ی نوشتنِ D1)
`IPV6_GROUP64` | `true` | IPv6 روی پیشوندِ /64 یک نقطه‌ی اتصال شمرده شود

کوتاه‌کردنِ `CONN_TTL` آزادسازیِ قطعیِ بی‌خبر را سریع‌تر می‌کند ولی اگر ضربان به
هر دلیل اجرا نشود سقف نشت می‌کند؛ بلندکردنش سقف را محکم‌تر می‌کند ولی یک اتصالِ
مرده‌ی بی‌خبر تا آن مدت جای خود را نگه می‌دارد.

### چند نکته‌ی عملی

- **IPv6:** کلاینت‌های IPv6 معمولاً آدرسِ موقتِ تصادفی می‌سازند (privacy
  extensions) که هر چند دقیقه عوض می‌شود. اگر هر آدرس جدا شمرده شود، *یک* دستگاه
  تنهایی سقف را پر می‌کند و کاربرِ قانونی بیرون می‌افتد. پس پیش‌فرض این است که
  یک /64 = یک نقطه‌ی اتصال — دقیقاً معادلِ همان کاری که NAT برای IPv4 می‌کند.
  برای جدا شمردنِ هر آدرس، `IPV6_GROUP64 = false` بگذارید.
- **دوگانه‌پشته (dual-stack):** اگر دامنه‌ی شما هم رکورد `A` و هم `AAAA` داشته
  باشد، ممکن است یک دستگاه یک‌بار با IPv4 و یک‌بار با IPv6 وصل شود و دو سقف
  مصرف کند. برای سقف‌های کوچک، یک خانواده‌ی آدرس را از دسترس خارج کنید.
- **بک‌اند (مهم‌ترین شرط):** شمارش فقط وقتی سراسری و دقیق است که یک مرجعِ مشترک
  بایند شده باشد — `LIMITER` (شیءِ ماندگار) یا `DB` (D1). بدون آن‌ها هر isolate
  حافظه‌ی خودش را می‌شمارد و **سقف عملاً اعمال نمی‌شود**. اگر مرجع بایند نباشد،
  پنل یک **بنرِ قرمز** بالای همه‌ی نماها نشان می‌دهد و کارتِ سلامت هم شکست
  می‌خورد؛ پس این وضعیت دیگر پنهان نمی‌ماند. وضعیتِ فعلی در
  **سلامت ورکر → مرجعِ شمارشِ محدودیت اتصال** و در `GET /health` (فیلدِ `limiter`) دیده می‌شود.
  راهنمای بایندکردن در بخشِ «🚨 مرحله‌ی حیاتی» بالای همین فایل است.
- **⚠️ «بایند بودن» با «کار کردن» یکی نیست:** اگر `DB` بایند باشد ولی جدولِ
  `conns` ساخته/خوانده نشود (پایگاهِ اشتباه بایند شده، DDL رد می‌شود، سهمیهٔ
  نوشتنِ D1 تمام است…) مسیرِ D1 شکست می‌خورد و شمارش **بی‌صدا** به حافظهٔ همان
  isolate می‌افتد. روی کلاودفلر دو دستگاه تقریباً همیشه به دو isolate مختلف
  می‌افتند، هر کدام فقط ۱ آی‌پی می‌بیند و سقف **هرگز پر نمی‌شود** — یعنی
  «هیچ بلاکی نمی‌شود» بدونِ هیچ خطای دیده‌شدنی. این حالت حالا صریح گزارش
  می‌شود: `/health` مقدارِ `limiter: "mem"` می‌دهد ولی `limiterIntended: "d1"`
  می‌ماند، `limiterDegraded: true` و `limitEnforced: false` می‌شود، متنِ خطا در
  `limiterError` می‌آید و کارتِ سلامت شکست می‌خورد. **اگر چراغ‌ها سبز بود ولی
  بلاکی ندیدید، اول همین دو فیلد را ببینید.**
- **تستِ داخلی:** در همان کارتِ سلامت، سناریوی «سقف آی‌پی برای اتصالِ بازِ
  بی‌ترافیک» همان باگِ بالا را زنده روی استقرارِ خودتان می‌سنجد.

### چرا این پیاده‌سازی با پنل‌های نوا و نهان فرق دارد؟

این بخش از مقایسه‌ی مستقیمِ کدِ مرجع نوشته شده است؛ برای عیب‌یابیِ همین
«محدودیت کار نمی‌کند» لازم شد.

| | [نوا](https://github.com/norayanmostafa/nova) | [نهان](https://github.com/itsyebekhe/nahan) | این پروژه |
|---|---|---|---|
| چی شمرده می‌شود | تعداد **IPهای متمایز** هر کاربر | تعداد **اتصالِ همزمان** هر کانفیگ | تعداد **IPهای متمایز** هر کاربر |
| کجا ذخیره می‌شود | `activeIps` **داخلِ رکوردِ کاربر** در همان کلیدِ KV پنل | `Map` در حافظهٔ پردازه | جدولِ جدا `conns` در D1 |
| نیاز به ساختِ schema | **نه** — همان کلیدِ موجودِ پنل | **نه** | **بله** (و پیش‌تر در هر اتصال تکرار می‌شد) |
| پنجرهٔ زمانی | ۲۰ ثانیه بی‌فعالیتی | طولِ عمرِ اتصال | ۹۰ ثانیه + ضربانِ ۴۵ ثانیه‌ای |
| سراسری؟ | بله (KV) | **نه** — فقط همین isolate | بله (D1) — *اگر واقعاً کار کند* |
| هنگامِ خطا | `sibatDchiyatChibur = 'ip-limit'` (دلیلِ نام‌دار) | — | **قبلاً بی‌صدا به حافظه می‌افتاد** ← باگِ اصلی |

**درسِ کلیدی:** نوا شمارنده را داخلِ همان رکوردی می‌گذارد که پنل از قبل
می‌خواند و می‌نویسد؛ پس **چیزی برای «ساخته‌شدن» ندارد که بی‌صدا شکست بخورد**.
این پروژه یک جدولِ جدا (`conns`) با DDL ساخت که اگر ساخته نشود، محدودیت بی‌صدا
از کار می‌افتد در حالی که همهٔ چراغ‌ها سبزند. رفعِ این پروژه:
۱) آزمونِ واقعیِ D1 با کشِ ۳۰ ثانیه‌ای (DDL دیگر در هر اتصال اجرا نمی‌شود)،
۲) گزارشِ صریحِ بک‌اندِ *کارکننده* به‌جای بک‌اندِ *بایندشده*،
۳) شکستِ کارتِ سلامت و بنرِ قرمز به‌جای سبزِ کاذب.

---

## 🔧 عیب‌یابی: «کانفیگ وصل نمی‌شود»

به‌ترتیب این موارد را بررسی کنید:

| مشکل | راه‌حل |
|---|---|
**SNI/Host اشتباه** | در **پروتکل و کانفیگ** فیلدهای `SNI` و `Host` را **خالی** بگذارید تا دامنه‌ی خود ورکر استفاده شود. اگر مقداری مثل `discordapp.com` باشد، کلاودفلر ترافیک را به سایت دیگری می‌فرستد و کانفیگ کار نمی‌کند.
**دامنه‌ی workers.dev فیلتر است** | به ورکر یک **دامنه‌ی اختصاصی** وصل کنید (Settings → Domains & Routes) و همان دامنه را در فیلد SNI بگذارید، یا از Clean IP به‌عنوان آدرس + دامنه‌ی خودتان به‌عنوان Host استفاده کنید.
**پروتکل خاموش است** | در تنظیمات، VLESS یا Trojan روشن باشد؛ اگر Shadowsocks/VMess فعال است بدانید که این دو در هسته‌ی تونل پیاده نشده‌اند (فقط در ساب تولید می‌شوند) — برای اتصال واقعی از VLESS یا Trojan استفاده کنید.
**UDP لازم دارید** | هسته‌ی فعلی فقط TCP را تونل می‌کند (دانلود و وب‌کامل کار می‌کند، بعضی بازی‌ها/تماس‌ها نه).
**مقصد فیلتر است** | در تنظیمات `Proxy IP` را پر کنید؛ خروجی از طریق آن relay می‌شود.
**Panic Mode فعال است** | دکمه‌ی قرمز `Panic` در نوار بالا را خاموش کنید.
**کاربر منقضی/غیرفعال** | در بخش کاربران وضعیت و سهمیه را بررسی کنید.

تست سلامت: `https://panel.<account>.workers.dev/health`

---

## 🗂 ساختار پروژه

```
worker.js        موتور: هسته‌ی تونل + API + سرویس اشتراک + امنیت (سورس — برای عیب‌یابی و بازسازی باندل)
_worker.obf.js   نسخهٔ نهایی و آمادهٔ دیپلوی (ضد-1101) — این را در داشبورد پیست کنید
wrangler.toml    ⚠️ بایندینگ‌های ورکر (LIMITER + DB) — حذفش سقفِ آی‌پی را بی‌صدا از کار می‌اندازد
ui/index.html    پوسته‌ی پنل (اسپرایت آیکون + جای تزریق CSS/JS)
ui/style.css     سیستم طراحی (تم تاریک/روشن، RTL، ریسپانسیو)
ui/app.js        منطق سمت کلاینت (۱۲ نما، فرم‌های اسکیمامحور)
ui/user.html     صفحهٔ وضعیت کاربر (پشتیبان new-subscription.html)
new-subscription.html  صفحهٔ وضعیت کاربر (نسخهٔ جدید — ورکر از گیت‌هاب می‌خواند)
tests-manual/    مجموعه‌ی تست محلی (بدون نیاز به حساب کلاودفلر)
```

### 🧪 اجرای تست‌ها

```bash
npm test                              # همه‌ی تست‌ها (اسکنر، پنل، بوت، HTML، سقفِ آی‌پی)
node tests-manual/tunnel-limit.cjs    # ★★ مسیرِ واقعیِ WebSocket — «چرا بلاک نمی‌شود؟»
node tests-manual/ip-limit.cjs        # ★ سقفِ آی‌پی + «D1 بایند است ولی کار نمی‌کند»
node tests-manual/worker-e2e.cjs      # صفحه‌ی کاربر + اسکنر (نسخه‌ی obfuscate‌شده)
node tests-manual/fixes-smoke.cjs     # توابعِ خالص (کانفیگ فیک، دروازه‌های سهمیه، پایپ‌لاین)
```

> ⚠️ تست‌ها باید با پسوند `.cjs` باشند؛ `package.json` این پروژه `type: module`
> است و فایل‌های `.js` به‌عنوان ESM خوانده می‌شوند (پس `require` در آن‌ها کار
> نمی‌کند). همه‌ی تست‌ها به `.cjs` تغییر نام داده‌اند و از `npm test` اجرا می‌شوند.

`tests-manual/ip-limit.cjs` ورکرِ واقعی (`worker.js`) را با یک **D1 واقعی**
(SQLite درون‌حافظه از `node:sqlite`) بالا می‌آورد و سناریوها را از مسیرِ رسمیِ
خودِ پنل (`POST /api/login` → `POST /api/action` با `{act:"usage-health"}`) اجرا
می‌کند — هیچ بخشی از منطقِ محدودیت در تست بازنویسی نشده است. پوشش:

1. سقفِ ۱ و ۲ آی‌پی، **اتصالِ بازِ بی‌ترافیک** (باگِ پنجره‌ی ۳ ثانیه‌ای)، شبکه‌ی
   ایمنیِ مهلتِ انقضا، و یکسان‌سازیِ آی‌پی (IPv4/IPv6)؛
2. **سناریوی «بدون بایندینگ»** — همان رگرسیونی که کامیتِ `360e05c` ساخت: ورکر
   بدون هیچ مرجعِ مشترکی بالا می‌آید و تست تأیید می‌کند که کارتِ سلامت این وضعیت
   را `mem` و «اعمال‌نشده» گزارش می‌کند (نه سبزِ کاذب)؛
3. در پایان، همین سناریو روی نسخه‌ای با پنجره‌ی باگ‌دارِ ۳ ثانیه اجرا می‌شود و
   انتظار می‌رود **شکست بخورد** — یعنی تست واقعاً باگ را می‌گیرد؛
4. **بخشِ F — «D1 بایند است ولی کار نمی‌کند»:** یک D1 جعلیِ خراب بایند می‌شود
   (هر عملیاتش `D1_ERROR` می‌دهد). تست تأیید می‌کند که `limiter` به `mem`
   تغییر می‌کند، `limiterIntended` روی `d1` می‌ماند، `limiterDegraded` روشن
   می‌شود، `limitEnforced` **false** می‌شود و کارتِ سلامت شکست می‌خورد — یعنی
   همان «افتِ بی‌صدای» معروف دیگر پنهان نمی‌ماند. همین بررسی روی باندلِ
   `_worker.obf.js` هم تکرار می‌شود.

`tests-manual/tunnel-limit.cjs` — **مسیرِ واقعیِ تونل.** یک `WebSocketPair`ِ کارا
می‌سازد، هندشیکِ معتبرِ VLESS می‌فرستد و همان مسیری را می‌آزماید که کاربر واقعاً
طی می‌کند:
`tunnelHandler → session() → dial() → clientIpOf → connAcquire → ws.close(1013)`.
پوشش: اتصالِ اول از IP اول → پذیرفته؛ اتصالِ دوم از **همان** IP → پذیرفته (سقف =
تعداد IP، نه تعداد اتصال)؛ اتصالِ سوم از **IP دیگر** → رد با کدِ **۱۰۱۳**؛
آزادسازی پس از بستن؛ سقفِ ۰ = نامحدود؛ و کلِ سناریو یک‌بار روی
`_worker.obf.js` (همان فایلی که روی کلاودفلر اجرا می‌شود).

> ⚠️ **با `grep` نمی‌توان فهمید باندلِ obfuscate‌شده درست است.** در
> `scripts/build-obfuscated.mjs` گزینهٔ `stringArray` با رمزنگاریِ `rc4` و
> `stringArrayThreshold: 1` روشن است، پس هیچ رشته‌ای (`connection limit
> reached`، `ipConnLimit`، …) به‌صورت خوانا در خروجی نیست و `grep` همیشه صفر
> می‌دهد. تنها راهِ معتبر **اجرای** باندل است — کاری که هر دو تست می‌کنند.

> نکتهٔ فنی: `Response`ِ استانداردِ Node وضعیتِ ۱۰۱ را رد می‌کند
> («must be in the range of 200 to 599») و `WebSocketPair` باید **iterable**
> باشد (ورکر با destructuring بازش می‌کند). هر دو در تست بدل شده‌اند؛ بدونِ آن
> مسیرِ تونل اصلاً اجرا نمی‌شد و تست بی‌معنا می‌ماند.

بقیه‌ی تست‌ها منطقِ `worker.js` را در محیطی شبیه‌سازی‌شده اجرا می‌کنند:
`cloudflare:sockets`، KV و شیءِ ماندگارِ محدودکننده با بدل‌های سبک جایگزین
شده‌اند.

> **فایل‌های حذف‌شده:** `subscription.html` و `index_unified_source_fixed.html` نسخه‌های قدیمیِ صفحهٔ کاربر بودند که هیچ ارجاعی به آن‌ها باقی نمانده بود — حذف شدند. مرجعِ فعلی فقط `new-subscription.html` (و پشتیبانِ آن `ui/user.html`) است.

---

## 📡 API

| متد | مسیر | توضیح |
|---|---|---|
`POST` | `/api/login` | ورود `{password, totp}` → توکن JWT-مانند
`GET` | `/api/state` | وضعیت کامل (هدر `Authorization: Bearer`)
`PUT` | `/api/settings` | ذخیره‌ی تنظیمات (merge عمیق)
`POST` | `/api/users` | ساخت کاربر یا `{id, op: "update\|toggle\|reset\|delete"}`
`POST` | `/api/usage` | ثبت مصرف `{"uuid","up","down"}`
`GET/POST/DELETE` | `/api/keys` | مدیریت کلیدهای API
`GET/POST/DELETE` | `/api/panels` | پنل‌های لینک‌شده
`POST` | `/api/action` | `panic`، `rotate-path`، `2fa-secret`، `pw-change`، `ui-refresh`، `domain-health`، `tg-test`، `update-check/deploy/rollback`، `factory`، `restore`
`GET` | `/sub/<uuid>` | اشتراک
`GET` | `/status/<name>` | صفحه‌ی وضعیت کاربر
`GET` | `/dns-query?name=&type=` | پروکسی DoH برای کلاینت‌ها
`GET` | `/health` | سلامت ورکر

```bash
curl https://panel.<account>.workers.dev/health

curl -X POST https://panel.<account>.workers.dev/api/login \
  -H "content-type: application/json" -d '{"password":"simorgh"}'
```

---

## ⚠️ محدودیت‌ها و نکات

- هسته‌ی تونل فقط **TCP** را پشتیبانی می‌کند (UDP نه).
- **Shadowsocks و VMess** فقط در ساب تولید می‌شوند و توسط هسته‌ی تونل پذیرفته نمی‌شوند.
- مصرف ثبت‌شده تقریبی است (در بستن هر اتصال به‌روزرسانی می‌شود).
- استفاده از این پروژه برای دور زدن تحریم/فیلترینگ مسئولیت خود شماست؛ لطفاً از قوانین محلی پیروی کنید.

## 📄 مجوز

MIT

</div>
